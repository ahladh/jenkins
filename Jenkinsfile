pipeline {
    agent any
    stages {
        stage('buildQ') {
            steps {
                sh './update.sh'
            }
            steps {
                sh './update.sh'
           }
        }
    }
}
