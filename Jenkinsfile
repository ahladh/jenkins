pipeline {
    agent any
    stages {
        stage('buildQ') {
            steps {
                sh './update.sh'
            }
        stage('Test') {
                sh './update.sh'
           }
        }
    }
}
